# Kalypso capabilities

Each Kalypso capability must be placed as `./capability/<capability-name>/version/*<manifests>*.yaml`. New versions increment the previous one until a major version is released.