# Implementation Plan - Kalypso High-Level Kubernetes CRDs

Rename the `Compute` capability into `Core` to align better with the generated resources it provides. Make sure to update all mentions, including docs.