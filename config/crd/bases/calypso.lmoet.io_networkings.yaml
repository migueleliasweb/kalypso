---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: networkings.calypso.lmoet.io
spec:
  group: calypso.lmoet.io
  names:
    kind: Networking
    listKind: NetworkingList
    plural: networkings
    singular: networking
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Networking is the Schema for the networkings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkingSpec defines the desired state of Networking
            properties:
              escapeHatches:
                description: EscapeHatches allows applying raw patches to the target
                  resource or managed Services/Ingresses.
                items:
                  description: EscapeHatch allows users to patch fields of resources
                    managed by the capability.
                  properties:
                    kind:
                      description: Kind is the kind of resource to patch (e.g. "Pod",
                        "Service", "Deployment", "HorizontalPodAutoscaler", etc.)
                      type: string
                    patch:
                      description: Patch is the patch string payload to apply (JSON
                        patch or Merge patch content).
                      type: string
                    patchType:
                      description: 'PatchType is the type of patch to apply. Supported
                        types: JSONPatch, JSONMergePatch.'
                      enum:
                      - JSONPatch
                      - JSONMergePatch
                      type: string
                  required:
                  - kind
                  - patch
                  - patchType
                  type: object
                type: array
              ingress:
                description: Ingress configures external access routes.
                properties:
                  privateRoutes:
                    description: PrivateRoutes configures Gateway API HTTPRoutes /
                      internal routes.
                    items:
                      description: GatewayRoute defines ingress routing parameters.
                      properties:
                        hostnames:
                          description: Hostnames for the route.
                          items:
                            type: string
                          type: array
                        paths:
                          description: Paths is a list of path prefixes or matches.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  publicRoutes:
                    description: PublicRoutes configures Gateway API HTTPRoutes /
                      public routes.
                    items:
                      description: GatewayRoute defines ingress routing parameters.
                      properties:
                        hostnames:
                          description: Hostnames for the route.
                          items:
                            type: string
                          type: array
                        paths:
                          description: Paths is a list of path prefixes or matches.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  tls:
                    description: TLS configuration.
                    properties:
                      secretName:
                        description: SecretName containing the TLS certificate.
                        type: string
                    type: object
                type: object
              service:
                description: Service configures the Service for the target workload.
                properties:
                  ports:
                    description: Ports is a list of routing ports.
                    items:
                      description: ServicePort contains information on service's port.
                      properties:
                        appProtocol:
                          description: |-
                            The application protocol for this port.
                            This is used as a hint for implementations to offer richer behavior for protocols that they understand.
                            This field follows standard Kubernetes label syntax.
                            Valid values are either:

                            * Un-prefixed protocol names - reserved for IANA standard service names (as per
                            RFC-6335 and https://www.iana.org/assignments/service-names).

                            * Kubernetes-defined prefixed names:
                              * 'kubernetes.io/h2c' - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-
                              * 'kubernetes.io/ws'  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455
                              * 'kubernetes.io/wss' - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455

                            * Other protocols should use implementation-defined prefixed names such as
                            mycompany.com/my-custom-protocol.
                          type: string
                        name:
                          description: |-
                            The name of this port within the service. This must be a DNS_LABEL.
                            All ports within a ServiceSpec must have unique names. When considering
                            the endpoints for a Service, this must match the 'name' field in the
                            EndpointPort.
                            Optional if only one ServicePort is defined on this service.
                          type: string
                        nodePort:
                          description: |-
                            The port on each node on which this service is exposed when type is
                            NodePort or LoadBalancer.  Usually assigned by the system. If a value is
                            specified, in-range, and not in use it will be used, otherwise the
                            operation will fail.  If not specified, a port will be allocated if this
                            Service requires one.  If this field is specified when creating a
                            Service which does not need it, creation will fail. This field will be
                            wiped when updating a Service to no longer need it (e.g. changing type
                            from NodePort to ClusterIP).
                            More info: https://kubernetes.io/docs/concepts/services-networking/service/#type-nodeport
                          format: int32
                          type: integer
                        port:
                          description: The port that will be exposed by this service.
                          format: int32
                          type: integer
                        protocol:
                          default: TCP
                          description: |-
                            The IP protocol for this port. Supports "TCP", "UDP", and "SCTP".
                            Default is TCP.
                          type: string
                        targetPort:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            Number or name of the port to access on the pods targeted by the service.
                            Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
                            If this is a string, it will be looked up as a named port in the
                            target Pod's container ports. If this is not specified, the value
                            of the 'port' field is used (an identity map).
                            This field is ignored for services with clusterIP=None, and should be
                            omitted or set equal to the 'port' field.
                            More info: https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service
                          x-kubernetes-int-or-string: true
                      required:
                      - port
                      type: object
                    type: array
                  type:
                    default: ClusterIP
                    description: Type of the Service (e.g. ClusterIP, NodePort, LoadBalancer).
                    type: string
                type: object
              targetRef:
                description: TargetRef references the target resource this capability
                  applies to.
                properties:
                  apiVersion:
                    description: ApiVersion is the API version of the resource (e.g.
                      "apps/v1").
                    type: string
                  kind:
                    description: Kind is the kind of the resource (e.g. "Deployment").
                    type: string
                  namespace:
                    description: Namespace is the namespace of the resource.
                    type: string
                  resource:
                    description: Resource is the name of the resource (e.g. "my-app").
                    type: string
                required:
                - apiVersion
                - kind
                - namespace
                - resource
                type: object
            type: object
          status:
            description: NetworkingStatus defines the observed state of Networking.
            properties:
              conditions:
                description: conditions represent the current state of the Networking
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
